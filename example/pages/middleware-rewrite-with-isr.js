import Layout from "../components/layout";

export default function Page() {
  return (
    <Layout>
      <article>
        <h1>Middleware — rewrite with Incremental Static Rendering (ISR)</h1>
        <hr />
        <p>❌ If you see this page, Middleware with rewrite is NOT working.</p>
      </article>
    </Layout>
  );
}
