id: assignment-to-handleNextImageRequest-in-constructor
language: typescript
rule:
  kind: assignment_expression
  pattern: this.handleNextImageRequest = $VALUE
  inside:
    kind: method_definition
    has:
      kind: property_identifier
      regex: ^constructor$
    inside:
      kind: class_body
